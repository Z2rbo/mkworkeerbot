# 🎨 Portfolio Bot + Website

Современное портфолио с Telegram ботом для сбора заявок и воронкой продаж.

## ✨ Особенности

### Сайт
- 🎯 Современный дизайн с параллакс-эффектами и анимациями
- 📱 Полностью адаптивный для всех устройств
- 🎨 Неоморфизм и плавные переходы
- 🔧 Админ-панель для добавления работ (Ctrl+Shift+A)
- 🖼️ Галерея работ с фильтрацией
- 📝 Форма обратной связи

### Telegram Бот
- 🤖 Воронка продаж с персонализацией
- 📋 Презентация услуг
- 📁 Показ портфолио
- 📝 Сбор заявок с уведомлениями администратору
- 💬 Умные ответы на вопросы

### Технологии
- **Frontend:** HTML5, CSS3, JavaScript (Vanilla)
- **Backend:** Python, FastAPI, SQLite
- **Bot:** python-telegram-bot
- **Деплой:** Vercel (сайт) + Railway (API + бот)

## 🚀 Быстрый старт

### Локальная разработка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/your-username/portfolio-bot.git
cd portfolio-bot
```

2. **Настройте окружение:**
```bash
cp config/.env.example config/.env
# Отредактируйте config/.env
```

3. **Установите зависимости:**
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

4. **Запустите API:**
```bash
python -m uvicorn api.main:app --reload --port 8000
```

5. **Запустите бота (в новом терминале):**
```bash
python -m bot.telegram_bot
```

6. **Откройте сайт:**
```bash
# Используйте Live Server в VS Code или:
cd frontend
python -m http.server 3000
```

### Docker

```bash
# Скопируйте .env файл
cp config/.env.example .env

# Запустите все сервисы
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

## 📦 Деплой

### Сайт на Vercel

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Деплой:
```bash
vercel
```

3. Настройте переменные окружения в Vercel Dashboard.

### API + Бот на Railway

1. Подключите репозиторий к Railway
2. Настройте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
   - `ADMIN_CHAT_ID` - ваш Telegram ID
   - `ADMIN_PASSWORD` - пароль для админ-панели
   - `SECRET_KEY` - случайный ключ для токенов
   - `WEBSITE_URL` - URL сайта на Vercel

3. Деплой происходит автоматически при push.

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | - |
| `ADMIN_CHAT_ID` | ID администратора в Telegram | - |
| `ADMIN_PASSWORD` | Пароль админ-панели | `portfolio_admin_2024` |
| `SECRET_KEY` | Секретный ключ для JWT | автогенерация |
| `DATABASE_PATH` | Путь к SQLite базе | `data/portfolio.db` |
| `UPLOAD_DIR` | Папка для загрузок | `uploads` |
| `WEBSITE_URL` | URL сайта | `https://your-portfolio.com` |

### Создание Telegram бота

1. Напишите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

### Получение Chat ID

1. Напишите @userinfobot в Telegram
2. Скопируйте ваш ID в `ADMIN_CHAT_ID`

## 📁 Структура проекта

```
portfolio-bot/
├── frontend/
│   ├── index.html          # Главная страница
│   ├── css/
│   │   ├── style.css       # Основные стили
│   │   └── animations.css  # Анимации
│   ├── js/
│   │   ├── app.js          # Основной JS
│   │   ├── works.js        # Логика работ
│   │   └── animations.js   # JS анимации
│   └── assets/
│       └── images/
├── backend/
│   ├── api/
│   │   └── main.py         # FastAPI приложение
│   ├── bot/
│   │   └── telegram_bot.py # Telegram бот
│   └── requirements.txt
├── config/
│   └── .env.example
├── docker-compose.yml
├── Dockerfile
├── Dockerfile.bot
├── vercel.json
├── railway.json
└── README.md
```

## 🔑 API Endpoints

### Публичные

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/works` | Список работ |
| GET | `/api/works/{id}` | Одна работа |
| POST | `/api/contact` | Отправить заявку |

### Требуют авторизации

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/works` | Добавить работу |
| PUT | `/api/works/{id}` | Обновить работу |
| DELETE | `/api/works/{id}` | Удалить работу |
| GET | `/api/contacts` | Список заявок |
| POST | `/api/admin/login` | Вход администратора |

## 🎨 Кастомизация

### Изменение цветов

Отредактируйте переменные в `frontend/css/style.css`:

```css
:root {
    --primary: #6366f1;      /* Основной цвет */
    --secondary: #10b981;    /* Вторичный цвет */
    --bg-dark: #0f0f1a;      /* Фон */
    --telegram: #0088cc;     /* Цвет Telegram */
}
```

### Добавление услуг

1. Отредактируйте `TEXTS` в `backend/bot/telegram_bot.py`
2. Добавьте секцию в `index.html`

## 📝 TODO

- [ ] Интеграция с аналитикой (Google Analytics, Yandex Metrika)
- [ ] Добавление мультиязычности
- [ ] Интеграция с платежами
- [ ] Добавление блога
- [ ] PWA поддержка

## 📄 Лицензия

MIT License - используйте свободно!

## 🤝 Поддержка

Есть вопросы? Создайте Issue или напишите в Telegram.

---

Made with ❤️ by Portfolio Team
